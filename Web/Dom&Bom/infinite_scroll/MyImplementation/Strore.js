function Store() {
    
}

// 每次异步获取count条数据
// 放回 长度为count 的 [item] 数组
Store.prototype.fetch = function(count) {

}

// 每次返回一个tombstone的dom结构 
Store.prototype.tombStone = function() {

}

// 根据item渲染出一个数据DOM, 
// dom 是复用的之前的数据DOM， 可能不存在
Store.prototype.render = function(item, dom) {

}